<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group
        android:id="@+id/menu_top"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_main_home"
            android:icon="@drawable/home"
            android:title="@string/menu_home"
            app:showAsAction="always"/>

        <item
            android:id="@+id/nav_main_chips"
            android:icon="@drawable/token"
            android:title="@string/menu_batting_history"
            app:showAsAction="always"/>

    </group>

    <group
        android:id="@+id/menu_bottom"
        android:checkableBehavior="single">


        <item
            android:id="@+id/nav_main_batting_history"
            android:icon="@drawable/history"
            android:title="@string/menu_batting"
            app:showAsAction="always"/>
        />
        <item
            android:id="@+id/nav_main_logout"
            android:icon="@drawable/logout"
            android:title="Logout"
            app:showAsAction="always"/>
    </group>
</menu>